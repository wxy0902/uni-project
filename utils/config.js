const baseUrl = ""; //域名
const miniappName = "e圈-敏捷信息共享咨询平台"; //站点标题 ，一般用于分享时的标题

const domain = 'https://' + baseUrl + "/api/"; //接口地址（无需更改）
export default {
	baseUrl,
	domain,
	miniappName,
}
